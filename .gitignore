# Build artifacts — regenerable with `make`. STLs ship via a GitHub Release;
# the gallery is rendered and published to GitHub Pages by the pages workflow.
build/
gallery/

# OS / editor cruft
.DS_Store
*~
*.swp

# Python
__pycache__/
*.pyc
